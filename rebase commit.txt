Trưowcs khi rebase commit chúng cần kiểm tra số lượng commit đang có trong máy bằng lệnh:
git log --oneline
số lượng commit được tính từ commit merged gần nhất
để rebase commit chúng ta sử dụng lệnh sau:
git rebase -i HEAD~sl commit
giữ pick đầu tiên từ pick thứ 2 đổi pick thành add
>Ctrl + X
>       Y
>Enter
giữ lại một commit các commit khác thêm # vào đầu tiên
>Ctrl + X
     Y
>Enter


